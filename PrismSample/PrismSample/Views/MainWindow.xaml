<Window x:Class="PrismSample.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="500" Width="525"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <StackPanel>
            <Label Content="システム日時" />
            <Label Content="{Binding SystemDateLabel}" />
            <Button Content="システム日時更新" Command="{Binding SystemDataUpdateButton}" />
            <Button Content="show viewA" Command="{Binding ShowViewAButton}" />
            <Button Content="show viewB" Command="{Binding ShowViewBButton}" />
            <Button Content="show viewC" Command="{Binding ShowViewCButton}" />
            <Button Content="show viewD" Command="{Binding ShowViewDButton}" />
            <ContentControl prism:RegionManager.RegionName="ContentRegion" />
        </StackPanel>

    </Grid>
</Window>
