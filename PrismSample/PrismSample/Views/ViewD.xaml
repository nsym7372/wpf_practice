<UserControl x:Class="PrismSample.Views.ViewD"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:bh="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
        <ListBox ItemsSource="{Binding Areas}" Height="100" Width="200" Margin="10" />
            <ComboBox ItemsSource="{Binding Products}" SelectedValuePath="Value" 
                      DisplayMemberPath="DisplayValue" Width="150" Margin="10" SelectedItem="{Binding SelectedProduct,Mode=TwoWay}">

                <bh:Interaction.Triggers>
                    <bh:EventTrigger EventName="SelectionChanged">
                        <prism:InvokeCommandAction Command="{Binding ProductsSelectionChanged}" TriggerParameterPath="AddedItems" />
                    </bh:EventTrigger>
                </bh:Interaction.Triggers>
            </ComboBox>
            <Label Content="{Binding SelectedText}" />
            <Button Content="OK" Command="{Binding CloseButton}" />
        </StackPanel>
    </Grid>
</UserControl>
