<Window x:Class="ValidationSample.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:local="clr-namespace:ValidationSample.ViewModels"
        Title="{Binding Title}" Height="350" Width="525" >
    <StackPanel>
        <TextBox>
            <TextBox.Text>
                <Binding Path="Message" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True">
                    <Binding.ValidationRules>
                        <local:StringLengthValidationRule MaxLength="5"/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>

        <Button Content="Click" Command="{Binding SubmitCommand}" />

        <!--<ContentControl prism:RegionManager.RegionName="ContentRegion" />-->
    </StackPanel>
</Window>
