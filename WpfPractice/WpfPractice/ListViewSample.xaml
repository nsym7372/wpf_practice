<Window x:Class="WpfPractice.ListViewSample"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfPractice"
        mc:Ignorable="d"
        Title="ListViewSample" Height="450" Width="600" Loaded="Window_Loaded">
    <Grid>
        <StackPanel Margin="10">
            <Button Name="BtAdd" Content="Add" FontSize="30" Click="BtAdd_Click" />
            <TextBox Name="TxSearch" FontSize="20" TextChanged="TxSearch_TextChanged" />
            <ListView Name="CustomerListView" Margin="0, 5, 0, 0" Height="250">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Id}" />
                            <TextBlock Text="{Binding Name}" FontSize="20" />
                            <TextBlock Text="{Binding Phone}" Foreground="Blue" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Content="Open" FontSize="30" Click="Button_Click" />
        </StackPanel>
    </Grid>
</Window>
